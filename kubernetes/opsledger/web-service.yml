apiVersion: v1
kind: Service
metadata:
  name: opsledger-web
  labels:
    app.kubernetes.io/name: opsledger
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: opsledger
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 8080
      targetPort: http
